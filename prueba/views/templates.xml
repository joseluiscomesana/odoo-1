<odoo>
  <data>
    <template id="report_visit_card">
      <t t-call="web.html_container">
        <t t-foreach="docs" t-as="o">
          <t t-call="web.external_layout">
            <div class="page">
              <h2>Visita</h2>
              <p>Cliente:
                <span t-field="o.customer.name"/>
              </p>
              <p>Descripción:
                <span t-field="o.name"/>
              </p>
              <p>Fecha:
                <span t-field="o.date" t-options='{"format":"dd/MM/yyyy"}' />
              </p>
            </div>
          </t>
        </t>
      </t>
    </template>
    <template id="index_test">
      <title>PRUEBA GET 2</title>
      si
      <p>hola... <t t-esc="visits"/></p>
      no
      <p>Modelo...: </p>
      <select id="modelos">
        <t t-foreach="visitas" t-as="visit">
          <li t-attf-value="{{visit.id}}"><span t-field="visit.name"/></li>
          no se
          <p>adios... <t t-esc="visit_id"/><t t-esc="visit"/></p>
        </t> 
      </select>
      llega
    </template>
  </data>
</odoo>
